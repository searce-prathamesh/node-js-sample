pipeline {
    agent any
    
    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
        
        stage('Cache NPM Dependencies') {
            steps {
                script {
                   
                        // Run npm install and cache the dependencies
   
                    }
                }
            }
        }

    }

