pipeline {
    agent any
    
    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
        
        stage('NPM Install') {
            steps {
                script {
                    // Run npm install
                    sh "npm install"
                }
            }
        }

        // Add more stages as needed for your build process
    }
}
