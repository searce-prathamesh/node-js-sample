pipeline {
  agent any 
  stages {
    stage('git Checkout') {
      steps {
        checkout scm
      }
    }
    stage('Install Dependencies') {
      steps {
        script {
          def yarnCacheDir = "${HOME}/.yarn-cache"
          sh "mkdir -p ${yarnCacheDir}"
          withEnv(["YARN_CACHE=${yarnCacheDir}"]) {
            sh 'yarn install'
          }
        }
      }
    }
    stage('Test') {
      steps {
        sh 'npm test' // Replace with your test command
      }
    }
  }
}

